/// <reference types="node" />
/// <reference types="node" />
import { Readable } from 'stream';
export declare class SSEEvent {
    data?: string;
    id?: string;
    event?: string;
    retry?: number;
    constructor(data?: {
        [key: string]: any;
    });
}
export default class TeaStream {
    static readAsBytes(stream: Readable): Promise<Buffer>;
    static readAsString(stream: Readable): Promise<string>;
    static readAsJSON(stream: Readable): Promise<any>;
    static readAsSSE(stream: Readable): AsyncGenerator<SSEEvent>;
}
